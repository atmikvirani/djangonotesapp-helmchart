apiVersion: v1
kind: Service
metadata:
  name: {{ include "notesappchart.fullname" . }}
  labels:
    {{- include "notesappchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "notesappchart.selectorLabels" . | nindent 4 }}
